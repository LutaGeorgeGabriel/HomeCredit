import React from "react";
import {Text, View} from "react-native";

const style = {
    flex: 1,
    paddingTop: 22
};

export const OthersScreen = () => (
    <View style={style}>
        <Text>Accounts</Text>
    </View>
);